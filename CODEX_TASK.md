# CODEX TASK — AQUA MOTIF GACHA を6,000候補まで増やす

このリポジトリの `AGENTS.md`、`CHAT_HANDOFF.md` と `BATCH_PROGRESS.json` を最初に読んでから、確認質問なしで作業してください。

## 目的
Splatoon系OCを今すぐ描くための、水生生物モチーフ10連ガチャです。図鑑としての完成度ではなく、実在するモチーフ候補を大量にすぐ抽選できることを最優先します。

**このタスクは6,000候補化専用レーンです。日本語化は別の `CODEX_JA_TASK.md` が担当します。**

## 今回の1タスク
1. コードから現在の候補総数を実測する。
2. `min(1000, 6000 - 現在候補数)` 件の新規候補を追加する。
3. 6,000を超えない。
4. 追加後にテスト・重複監査を行う。
5. `BATCH_PROGRESS.json` と `CHAT_HANDOFF.md` を実数で更新する。
6. 変更を完成状態にし、可能ならPRを作成する。

この同じ指示を別タスクで再実行できるようにすること。すでに6,000件なら追加せず監査だけ行う。

## 並行日本語化との衝突回避
この6,000候補化タスクでは、原則として以下を変更しないでください。
- `data/ja-names.js`
- `CODEX_JA_TASK.md`
- `JA_PROGRESS.json`
- `JA_HANDOFF.md`

新しい候補に日本語名が分かる場合でも、このレーンでは候補追加を優先し、日本語別名マップの整備は日本語化レーンへ任せて構いません。

## 候補ルール
- 哺乳類を除く実在する水生動物。
- 魚類、サメ・エイ、甲殻類、貝、頭足類、ウミウシ、クラゲ、サンゴ、イソギンチャク、棘皮動物、海綿、ホヤ、多毛類、その他の水生無脊椎動物、淡水生物、水生昆虫など。偏りは気にしなくてよい。
- 日本語一般名が確実なら日本語を優先。
- 日本語名が不確実なら一般的な英名または学名をそのまま使ってよい。
- 架空名を作らない。
- 確認できない日本語名を捏造しない。
- 説明・分類・タグは不要。名前だけでよい。
- 完全な同一種、単なる別名、日本語名と学名で同じ種を意図的に二重登録することは避ける。
- 極小の近縁差や分類上の偏りは厳密に整理しなくてよい。

### 水増し禁止 — 最重要
候補数は「文字列の違い」ではなく、**別モチーフとして成立する実在タクソン/生物**を増やしてください。

以下は禁止です。
- 既存の属名 `Carcharhinus` に `Carcharhinus sp.` を追加するような、未同定表記だけの別候補化。
- `sp.` / `spp.` / `cf.` / `aff.` / `?` などを付けただけの暫定・未同定・不確定表記。
- 同じ学名の括弧・空白・大文字小文字・著者名・年号などの表記差だけで別候補にすること。
- 既存の属と、その属そのものを意味する別表記を重ねて候補数を増やすこと。
- 同じ種の和名・英名・学名を別々の抽選候補として重複登録すること。

属名が既に候補にあっても、その属に属する**具体的な実在種**（例: `Genus species`）を追加することは可能です。具体的な種は属候補とは別モチーフとして扱って構いません。ただし、追加する二名法が実在する種名であることを確認してください。

新規1,000件は、可能な限り具体的な種（binomial）または独立した標準的な一般名を優先してください。

## データ
現在は `data/species-*.js` が候補名の正本です。既存候補を消さずに追加してください。

必要なら新規バッチを別ファイルにして構いません。その場合は `index.html` から必ず読み込ませてください。

アプリ実行時に `window.AQUA_SPECIES` に全候補が入る構造を維持してください。

## 抽選
- 1回10件。
- 同一10連内の重複なし。
- 基本は全候補から一様ランダムでよい。
- 近縁グループ重み付けなど複雑な仕様は不要。

## iPhone最優先
GitHub Pages + iPhone Safariで使う静的サイトです。

- サーバー/API不要。
- 横スクロール禁止。
- Safe Areaを壊さない。
- ボタンはタップしやすくする。
- JSエラーで結果が空になる状態を作らない。
- 10件コピーと前回結果保存を維持する。
- 外部CDNは不要。
- 日本語表示レイヤー `data/ja-names.js` を壊さない。

## 必須監査・テスト
候補の正規化キーを `NFKC -> trim -> lower-case` として、最低限以下を自動確認してください。

1. 予定候補数と実数が一致。
2. 6,000以下。
3. 空名0。
4. 正規化完全重複0。
5. 新規候補に `sp.` / `spp.` / `cf.` / `aff.` などの未同定・暫定表記0。
6. 新規学名候補が、既存名へ未同定接尾辞を付けただけの擬似差分になっていない。
7. 10件抽選で10件すべて異なる。
8. 抽選を多数回実行して例外なし。
9. 全JSの構文エラー0。
10. ページ読込時の致命的エラー0。
11. iPhone相当の狭いviewportで横スクロールしない。
12. 日本語別名ファイルを読み込んだ状態でも抽選が壊れない。

重複や無効候補を見つけたら、別の実在候補で置き換えて最終件数を予定値に合わせてください。

## 進捗記録
`BATCH_PROGRESS.json` を最新の実数で更新してください。

`CHAT_HANDOFF.md` には次の担当がそれだけ読めば続けられるよう、現在件数、残り、今回の変更、追加ファイル、テスト結果、既知の問題、次の作業を書いてください。

## 完了報告
最後に必ず以下を簡潔に報告してください。
- 作業前候補数
- 追加件数
- 作業後候補数
- 重複件数
- `sp.` 等の禁止表記件数（0であること）
- テスト結果
- 6,000までの残り

最重要: 架空の生物名や表記揺れで数を埋めないこと。説明文作りには時間を使わず、**実在する別モチーフ候補**の追加と動作確認を優先してください。
