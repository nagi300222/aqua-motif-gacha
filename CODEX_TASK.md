# CODEX TASK — AQUA MOTIF GACHA を6,000候補まで増やす

最初に最新 `main` を基準にし、`AGENTS.md`、`CHAT_HANDOFF.md`、`BATCH_PROGRESS.json` を読んでください。過去の失敗PR/古いCodexブランチを土台にしないでください。

## 目的
Splatoon系OCを今すぐ描くための、水生生物モチーフ10連ガチャです。図鑑メタデータより、実在する別モチーフ候補を速く増やすことを優先します。

このタスクは **6,000候補化専用レーン** です。日本語化は `CODEX_JA_TASK.md` が担当します。

## 完了ゲート — 最重要
作業開始時に候補数を実測し、`start_count` としてください。

今回追加すべき件数は必ず:

`add_count = min(1000, 6000 - start_count)`

作業終了時は必ず:

`final_count = start_count + add_count`

になっていなければ **タスク未完了** です。

現在mainが2,304件なら、このタスクの完成条件は **+1,000件して3,304件** です。

監査スクリプト追加、既存データ整理、失敗バッチ削除だけで終了してはいけません。`added_this_batch: 0` で完了扱いにしてはいけません。

## 今回やること
1. 最新mainから現在候補数を実測。
2. `min(1000, 6000-current)` 件の **新しい実在する具体種** を追加。
3. 原則として新規追加は、確認できる二名法 `Genus species` の学名を使う。
4. 新規データファイルを作り、`index.html` で `data/ja-names.js` より前、`app.js` より前に読み込む。
5. `node scripts/audit-species.js <final_count>` を含む監査を実行。
6. `BATCH_PROGRESS.json` と `CHAT_HANDOFF.md` を実数更新。
7. 最新mainの日本語表示基盤を保持したままPRを作成。

## 候補ルール
- 哺乳類を除く実在する水生動物。
- 魚類、サメ・エイ、甲殻類、貝、頭足類、ウミウシ、刺胞動物、棘皮動物、海綿、ホヤ、多毛類、淡水生物、水生昆虫など。偏りは問わない。
- **今回の新規候補は具体種の二名法を優先する。属名だけを新規追加して数を埋めない。**
- 架空学名を作らない。実在確認できない候補は捨てて別の実在種に置換する。
- 説明・分類・タグは不要。
- 既存の属候補と、その属の具体種 `Genus species` は併存してよい。
- 同じ種の和名・英名・学名を別候補として重ねない。

## 水増し禁止
以下は新規候補として禁止です。
- `Genus sp.` / `Genus spp.`
- `cf.` / `aff.` / `?` を含む未同定・暫定表記
- 既存名への記号・空白・大文字小文字・著者名・年号追加だけの差分
- 同じタクソンの別表記だけの追加
- 既存1,000件へ接尾辞を機械的に付けて別候補化すること

## 並行日本語化との衝突回避
このレーンでは変更しない:
- `data/ja-names.js`
- `CODEX_JA_TASK.md`
- `JA_PROGRESS.json`
- `JA_HANDOFF.md`

`index.html` を変更する場合、既存の `data/ja-names.js` 読み込みと日本語表示基盤を絶対に削除しないでください。

## 必須監査
最低限:
1. `final_count === start_count + add_count`
2. 6,000以下
3. 空名0
4. NFKC → trim → lower-case の完全重複0
5. `sp.` / `spp.` / `cf.` / `aff.` / `?` の禁止表記0
6. 今回追加した学名が原則として具体的な二名法
7. 10連を10,000回実行し各回10件一意、例外0
8. 全JS構文エラー0
9. ページ初期化成功
10. iPhone狭幅で横スクロールなし
11. 日本語表示ファイル込みで抽選正常

## 進捗
`BATCH_PROGRESS.json` は実数で更新し、最低限 `target`, `current`, `added_this_batch`, `remaining` を正しくする。

`CHAT_HANDOFF.md` には作業前件数、追加件数、作業後件数、追加ファイル、テスト結果、残り、次の作業を書く。

## 完了報告
必ず:
- 作業前候補数
- 追加件数
- 作業後候補数
- 重複件数
- 禁止表記件数
- テスト結果
- 6,000まで残り

を報告してください。

**件数が完成条件に届いていなければPRを完成扱いにせず、そのまま追加作業を続けてください。**
