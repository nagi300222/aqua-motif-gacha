# CODEX JAPANESE TASK — AQUA MOTIF GACHA 日本語表示を並行拡充する

最初に最新 `main` を基準にし、`AGENTS.md`、`JA_PROGRESS.json`、`JA_HANDOFF.md` を読んでから、確認質問なしで日本語化レーンだけを進めてください。過去の失敗PRや古いCodexブランチを土台にしないでください。

## 目的
6,000候補化を止めずに、既存の英名・学名候補へ**日本の公的・学術的資料で確認できた日本語名だけ**を別マップとして追加します。

ガチャ候補そのものは変更しません。`window.AQUA_SPECIES` の件数・順序・内容をこのタスクでは触らないでください。

日本語化率より正確さを優先します。候補を未翻訳のまま残すことは失敗ではありません。誤った和名を付けることが失敗です。

## 正本
日本語表示マップは `data/ja-names.js` の `window.AQUA_JA_NAMES` です。

```js
window.AQUA_JA_NAMES=window.AQUA_JA_NAMES||{};
Object.assign(window.AQUA_JA_NAMES,{
  "Scarus":"ブダイ属"
});
```

キーは `window.AQUA_SPECIES` に存在する元候補名と完全一致させてください。

各aliasの確認元は、UIでは読み込まない監査用ファイル `data/ja-sources.json` に記録してください。

例:

```json
{
  "Euprymna berryi": "https://www.godac.jamstec.go.jp/..."
}
```

`data/ja-names.js` に存在する各キーには、原則 `data/ja-sources.json` に1件以上の確認元URLが必要です。

## 1タスクで行うこと
1. 全 `data/species-*.js` を読み、現在候補数を実測する。
2. 既に日本語を含む候補は `builtin_japanese` として数えるが、原則マップへ重複登録しない。
3. `data/ja-names.js` の既存マッピングを数える。
4. **既存マッピングも含めて誤対応がないか監査する。既知の誤りを見つけたら先に修正する。**
5. 未対応候補から、1タスクあたり**最大200件程度**を目安に、資料で確認できた日本語名だけ追加する。
6. 件数を埋めるために推測・機械翻訳・代表種からの類推をしない。確認できないものはスキップする。
7. `data/ja-sources.json`、`JA_PROGRESS.json`、`JA_HANDOFF.md` を実数で更新する。
8. テスト後、変更をコミットし、可能ならPRを作成する。

## 最重要ルール
- 日本語名を捏造しない。
- モデルの記憶だけで日本語名を確定しない。
- 単なる機械翻訳やカタカナ転写を「和名」として登録しない。
- 学名の属を、その属に含まれる代表的な1種の和名へ置き換えない。
- 元候補が属なら、日本語も同じ分類階級の確立した属名（例: `〜属`）が資料で確認できる場合のみ対応する。
- 元候補が種なら、その**同一種**に対応する確立した標準和名・一般和名を使う。
- 旧学名・シノニムと現行名を取り違えない。資料上で同一タクソンだと明示確認できる場合のみ採用する。
- 同名の別分類群を取り違えない。
- 元候補がすでに日本語なら、そのままでよく、別名へ勝手に置換しない。
- 情報源間で和名またはタクソン対応が競合する場合、日本の公的・学術系の現行データを優先し、解消できなければ未対応のまま残す。

## 情報源の優先順位
**ネットワーク検索を実際に使って確認してください。** 確認できない場合は登録しないでください。

優先:
1. JAMSTEC BISMaL / GODAC
2. 国立科学博物館 FishPix / UODAS / 標本・淡水魚類データベース
3. 東京大学など大学博物館・公的研究機関の標本DB
4. J-STAGE掲載の査読論文、学会資料
5. 日本の水族館・博物館・研究機関の公式資料
6. 上記が見つからない場合のみ FishBase / WoRMS 等を補助的に使用

Wikipedia、一般ブログ、ECサイト、まとめサイトだけを根拠に登録しないでください。

FishBase等と日本の公的資料が食い違う場合は、日本の公的・学術系資料を優先してください。

## 既知の回帰テスト
過去の日本語化PRで実際に誤対応が発生したため、以下を必ず回帰確認してください。

- `Arothron meleagris` は `モヨウフグ` にしない。日本資料では `ミゾレフグ`。
- `Arothron stellatus` が対象なら `モヨウフグ`。
- `Euprymna berryi` は `ミミイカ` にしない。`ニヨリミミイカ`。
- `Euprymna morsei` が対象なら `ミミイカ`。
- `Gymnothorax kidako` は `トラウツボ` にしない。`ウツボ`。
- `Enchelycore pardalis` が対象なら `トラウツボ`。
- `Gymnothorax favagineus` は、現在の国立科学博物館FishPixでは `ヒョウモンウツボ` として確認される。古い/別DBの `ニセゴイシウツボ` を無検証で採用しない。
- `Turritopsis dohrnii` は `ベニクラゲ` とだけ登録しない。JAMSTEC BISMaLでは `チチュウカイベニクラゲ`。
- `Carassius auratus` を `ギンブナ` と直接対応させない。国立科学博物館ではギンブナを `Carassius auratus langsdorfii` / `Carassius langsdorfii` と区別している資料があるため、分類対応を厳密に確認する。

これらは単なる例ではなく、同種の取り違えを防ぐための回帰テストです。

## UI仕様
UI側はすでに次の挙動に対応しています。
- 日本語マップあり: 日本語名を大きく表示し、元の英名/学名を小さく併記。
- 日本語マップなし: 元名をそのまま表示。
- コピー時: `日本語名（元名）` の形。

この日本語化タスクでは、原則 `app.js` と `index.html` を変更しないでください。表示基盤に実害のあるバグを見つけた場合のみ、理由を `JA_HANDOFF.md` に記録して最小修正してください。

## 並列作業の衝突回避
このタスクでは以下を変更しないでください。
- `data/species-*.js`
- `BATCH_PROGRESS.json`
- `CHAT_HANDOFF.md`
- 6,000件化の候補データ

日本語化レーンが通常変更してよいのは:
- `data/ja-names.js`
- `data/ja-sources.json`
- `JA_PROGRESS.json`
- `JA_HANDOFF.md`

これにより、6,000件化Codexタスクと同時進行できるようにします。

## 必須監査
最低限以下を確認してください。
1. 日本語化前後で `window.AQUA_SPECIES` の候補数・順序・内容が変化していない。
2. `AQUA_JA_NAMES` の全キーが実在する候補名に完全一致している。
3. マップ値が空文字でない。
4. 同一キーの意図しない上書きがない。
5. 日本語値に日本語文字が含まれる。
6. 各aliasについて `data/ja-sources.json` に確認元がある。
7. 追加・修正したaliasは、その確認元に**同じ学名（または明示された同一タクソンのシノニム）と日本語名の対応**が実際に掲載されている。
8. 上記「既知の回帰テスト」に違反しない。
9. JS/JSON構文エラー0。
10. マップを読み込んでも10連抽選が正常に動く。
11. 日本語あり/なし双方の表示・コピー処理で例外がない。

## 進捗
`JA_PROGRESS.json` を実数更新してください。

```json
{
  "candidate_total": 2304,
  "builtin_japanese": 604,
  "verified_aliases": 0,
  "localized_total": 604,
  "unlocalized": 1700,
  "added_this_task": 0
}
```

数値は例なので、必ずその時点の最新mainを実測してください。

`localized_total` は「元名自体が日本語の候補 + `AQUA_JA_NAMES` で日本語対応済みの候補」の重複なし合計です。

## 完了報告
- 現在候補数
- 元から日本語の候補数
- 作業前マッピング数
- 今回修正した誤対応数
- 今回追加した日本語マッピング数
- 日本語対応済み総数 / 候補総数
- 未対応数
- 全aliasのsource coverage
- 監査結果
- 不確かでスキップした代表例

**日本語化率より正確さを優先してください。資料で確認できなければ学名のまま残してください。**
