# CODEX JAPANESE TASK — AQUA MOTIF GACHA 日本語表示を並行拡充する

最初に `AGENTS.md`、`JA_PROGRESS.json`、`JA_HANDOFF.md` を読み、確認質問なしで日本語化レーンだけを進めてください。

## 目的
6,000候補化を止めずに、既存の英名・学名候補へ「確認できる日本語名」を別マップとして追加します。

ガチャ候補そのものは変更しません。`window.AQUA_SPECIES` の件数・順序・内容をこのタスクでは触らないでください。

## 正本
日本語表示マップは `data/ja-names.js` の `window.AQUA_JA_NAMES` です。

形式例:

```js
window.AQUA_JA_NAMES=window.AQUA_JA_NAMES||{};
Object.assign(window.AQUA_JA_NAMES,{
  "Scarus":"ブダイ属"
});
```

キーは `window.AQUA_SPECIES` に存在する元の候補名と完全一致させてください。

## 1タスクで行うこと
1. 全 `data/species-*.js` を読み、現在候補数を実測する。
2. 既に日本語を含む候補は `builtin_japanese` として数えるが、原則マップへ重複登録しない。
3. `data/ja-names.js` の既存マッピングを数える。
4. 未対応の英名・学名候補から、**最大500件を目安**に、確実な日本語名だけ追加する。
5. 500件を埋めるために不確かな訳を作らない。確信できないものはスキップしてよい。
6. `JA_PROGRESS.json` と `JA_HANDOFF.md` を実数で更新する。
7. テスト後、変更をコミットし、可能ならPRを作成する。

## 最重要ルール
- 日本語名を捏造しない。
- 単なる機械翻訳やカタカナ転写を「和名」として登録しない。
- 学名の属を、その属に含まれる代表的な1種の和名へ置き換えない。
- 元候補が属なら、日本語も同じ分類階級の確立した属名（例: `〜属`）が確認できる場合のみ対応する。
- 元候補が種なら、その種に対応する確立した標準和名・一般和名を使う。
- 同名の別分類群を取り違えない。
- 元候補がすでに日本語なら、そのままでよく、別名へ勝手に置換しない。

## 情報源
ネットワーク・検索が利用可能なら、分類同定と日本語名の確認には信頼できる一次・準一次資料を優先してください。日本語の公的・学術的な生物データベース、博物館・水族館・研究機関、学会資料などを優先します。

Wikipedia等しか確認できない場合は、分類階級と対応関係を慎重に確認してください。不確かなものは未対応のまま残してください。

## UI仕様
UI側はすでに次の挙動に対応しています。
- 日本語マップあり: 日本語名を大きく表示し、元の英名/学名を小さく併記。
- 日本語マップなし: 元名をそのまま表示。
- コピー時: `日本語名（元名）` の形。

この日本語化タスクでは、原則 `app.js` と `index.html` を変更しないでください。表示基盤に実害のあるバグを見つけた場合のみ、理由を `JA_HANDOFF.md` に記録して最小修正してください。

## 並列作業の衝突回避
このタスクでは以下を変更しないでください。
- `data/species-*.js`
- `BATCH_PROGRESS.json`
- `CHAT_HANDOFF.md`
- 6,000件化の候補データ

日本語化レーンが通常変更してよいのは:
- `data/ja-names.js`
- `JA_PROGRESS.json`
- `JA_HANDOFF.md`

これにより、6,000件化Codexタスクと同時進行できるようにします。

## 必須監査
最低限以下を確認してください。
1. 日本語化前後で `window.AQUA_SPECIES` の候補数が変化していない。
2. `AQUA_JA_NAMES` の全キーが実在する候補名に一致している。
3. マップ値が空文字でない。
4. 同一キーの意図しない上書きがない。
5. 日本語値に日本語文字が含まれる。
6. JS構文エラー0。
7. マップを読み込んでも10連抽選が正常に動く。
8. 日本語あり/なし双方の表示処理で例外がない。

## 進捗
`JA_PROGRESS.json` を次のような形で実数更新してください。

```json
{
  "candidate_total": 2304,
  "builtin_japanese": 0,
  "verified_aliases": 0,
  "localized_total": 0,
  "unlocalized": 2304,
  "added_this_task": 0
}
```

`localized_total` は「元名自体が日本語の候補 + `AQUA_JA_NAMES` で日本語対応済みの候補」の重複なし合計です。

## 完了報告
- 現在候補数
- 元から日本語の候補数
- 作業前マッピング数
- 今回追加した日本語マッピング数
- 日本語対応済み総数 / 候補総数
- 未対応数
- 監査結果
- 不確かでスキップした代表例があれば数件

日本語化率より正確さを優先してください。
