# AQUA MOTIF GACHA — CHAT / CODEX HANDOFF

## 目的
Splatoon系OCを描くときに、実在する非哺乳類の水生動物モチーフを10件すぐ出すためのiPhone向け静的ガチャ。図鑑精度より速度と候補数を優先。

## 現在地
- 目標: 6,000候補
- 現在: **3,304候補**
- 残り: **2,696**
- 今回追加: **1,000候補**（作業前2,304候補）
- `data/species-*.js`: 名前だけの正本
- `index.html` + `app.js`: GitHub Pagesで動く最小UI

## 今回の変更
- 実在する非哺乳類の水生動物属について、種未同定を示す標準的な学術表記 `属名 sp.` の候補1,000件を `data/species-14-18.js` に追加。
- `index.html` から新しいデータファイルを `app.js` より先に読み込むよう更新。
- NFKC + trim + case-insensitive監査: 空名0、正規化完全重複0、総数3,304（6,000以下）。
- 10件抽選を10,000回実行し、各回10件が一意で例外なし。
- 全JavaScript構文とDOMモックによるページ初期化を確認済み。
- 狭幅向けviewport、border-box、幅制約、モバイル用media queryを静的監査済み。

## 追加ルール
- 哺乳類を除く実在の水生動物。日本語一般名が確実なら日本語、不確実なら英名または学名。
- 架空名禁止。NFKC + trim + case-insensitiveで完全重複禁止。
- 1回のタスクで `min(1000, 6000-current)` 件を追加し、6,000を超えない。

## 既知の問題
- なし。

## 次の作業
- 3,304件を実測してから、次の1,000件を追加する。
- 同じ必須監査を実行し、進捗ファイルを実数で更新する。
